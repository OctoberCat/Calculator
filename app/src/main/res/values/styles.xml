<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyCustomTabText</item>
        <item name="tabSelectedTextColor">@color/tab_text_act</item>
    </style>

    <style name="MyCustomTabText" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>


    <!--login and related theme-->
    <style name="AppTheme.Login">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

        <item name="android:textColorHint">@android:color/white</item>
        <!--<item name="textColorHint">@android:color/white</item>-->
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorControlHighlight">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>

        <!--<item name="android:colorAccent">@android:color/white</item>-->
        <item name="android:windowBackground">@color/colorPrimary</item>

        <item name="android:buttonStyle">@style/Widget.Button.LoginBtn</item>
        <item name="buttonStyle">@style/Widget.Button.LoginBtn</item>
        <item name="android:editTextStyle">@style/Widget.EditText.LoginEditText</item>
    </style>

    <style name="Widget.Button.LoginBtn" parent="android:Widget.Button">
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:height">50dp</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">15dp</item>
    </style>

    <style name="Widget.EditText.LoginEditText" parent="android:Widget.EditText">
        <!--<item name="app:backgroundTint">@android:color/white</item>-->
        <!--<item name="android:textColor">@android:color/white</item>-->
        <!--<item name="android:textColorHint">@android:color/white</item>-->
        <!--<item name="textColorHint">@android:color/white</item>-->
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorControlHighlight">@android:color/white</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:padding">10dp</item>
    </style>
</resources>
